{
  "dependencies": {
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
   "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "deploy": "npx vite build && npx gh-pages -d dist"

  }
}
